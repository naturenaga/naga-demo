on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy to'
        required: true
        type: choice
        options:
          # - dev
          - qa
          - prod

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Deploy to Dev
        # if: github.event.inputs.environment == 'dev'
        run: |
          # Deploy to Dev environment
          echo "Deploying to Dev environment..."

      - name: Wait for QA approval
        if: github.event.inputs.environment == 'qa'
        run: |
          echo "Waiting for manual approval for QA deployment"
          # Use a timeout or a manual approval mechanism here

      - name: Deploy to QA
        if: github.event.inputs.environment == 'qa'
        run: |
          # Deploy to QA environment
          echo "Deploying to QA environment..."

      - name: Wait for Prod approval
        if: github.event.inputs.environment == 'prod'
        run: |
          echo "Waiting for manual approval for Prod deployment"
          # Use a timeout or a manual approval mechanism here

      - name: Deploy to Prod
        if: github.event.inputs.environment == 'prod'
        run: |
          # Deploy to Prod environment
          echo "Deploying to Prod environment..."
